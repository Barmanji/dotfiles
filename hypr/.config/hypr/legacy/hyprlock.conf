source = $HOME/.config/hypr/mocha.conf

$accent = $mauve
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

general {
    disable_loading_bar = true
        hide_cursor = true
        ignore_empty_input = true
        immediate_render = true
}

background {
    monitor =
        path = /home/barmanji/Downloads/ColorWall/wallhaven-yq752d.jpg
# color = rgba(25, 20, 20, 1.0)

# all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
        blur_size = 4
        blur_passes = 4 # 0 disables blurring
# noise = 0
# contrast = 1
# brightness = 1
# vibrancy = 1
# vibrancy_darkness = 1
}

input-field {
    monitor =
        size = 300, 30
        outline_thickness = 0
        dots_size = 0.25 # Scale of input-field height, 0.2 - 0.8
        dots_spacing = 0.55 # Scale of dots' absolute size, 0.0 - 1.0
        dots_center = true
        dots_rounding = -1
        outer_color = rgba(242, 243, 244, 0)
        inner_color = rgba(242, 243, 244, 0)
        font_color = rgba(242, 243, 244, 0.75)
        fade_on_empty = false
        placeholder_text = # Text rendered in the input box when it's empty.
        hide_input = false
        check_color = rgba(204, 136, 34, 0)
        fail_color = rgba(204, 34, 34, 0) # if authentication failed, changes outer_color and fail message color
        fail_text = $FAIL <b>($ATTEMPTS)</b> # can be set to empty
        fail_transition = 300 # transition time in ms between normal outer_color and fail_color
        capslock_color = -1
        bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
        invert_numlock = false # change color if numlock is off
        swap_font_color = false # see below
        position = 0, -468
        halign = center
        valign = center
}

# Battery
label {
    monitor =
        text = cmd[update:1000] $HOME/scripts/battery_icon.sh
        color = $green
        text_align = center
        font_size = 18
        font_family = $font
        rotate = 0
        position = 0, 215
        halign = center
        valign = center
}
label {
    monitor =
        text = cmd[update:1000] echo "󱎫 $(grep "$(date +%Y-%m-%d)" $HOME/scripts/study_timer/total_time_perDay.log | tail -n 1 | awk -F': ' '{print $2}' | awk '{print $1, $2}')" | sed 's/^$/00h 00m/'
        color = $mauve
        text_align = center
        font_size = 21
        font_family = $font
        rotate = 0
        position = 0, 180
        halign = center
        valign = center
}
# Date & Time
label {
    monitor =
        text = cmd[update:1000] echo "$(date +"%A, %B %d")"
        color = rgba(242, 243, 244, 0.75)
        font_size = 20
        font_family = SF Pro Display Bold
        position = 0, 405
        halign = center
        valign = center
}

label {
    monitor =
        text = cmd[update:1000] echo "$(date +"%k:%M")"
        color = rgba(242, 243, 244, 0.75)
        font_size = 93
        font_family = SF Pro Display Bold
        position = 0, 310
        halign = center
        valign = center
}

# Name
label {
    monitor =
        text = Barmanji
        color = rgba(242, 243, 244, 0.75)
        font_size = 12
        font_family = SF Pro Display Bold
        position = 0, -407
        halign = center
        valign = center
}

# Input
label {
    monitor =
        text = Enter Password
        color = rgba(242, 243, 244, 0.75)
        font_size = 10
        font_family = SF Pro Display
        position = 0, -438
        halign = center
        valign = center
}

label {
    monitor =
        text = cmd[update:1000] $HOME/.config/hypr/scripts/Hyprsunset_Auto_NightLight8PM.sh
        font_size = 0
}

#
# Add persnol image
# image {
#     monitor =
#     path = /home/barmanji/Downloads/ColorWall/wallhaven-vg7lv3.jpg
#     border_color = 0xffdddddd
#     border_size = 0
#     size = 73
#     rounding = -1
#     rotate = 0
#     reload_time = -1
#     reload_cmd =
#     position = 0, -353
#     halign = center
#     valign = center
# }
# label {
#   monitor =
#   text = cmd[update:1000] echo "$(~/.config/Scripts/song-status)"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 14
#   font_family = SF Pro Text
#   position = 20, 512
#   halign = left
#   valign = center
# }
#
# label {
#   monitor =
#   text = cmd[update:1000] echo "$(~/.config/Scripts/network-status)‎"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 16
#   font_family = SF Pro Text
#   position = -35, 512
#   halign = right
#   valign = center
# }
#
#
# label {
#   monitor =
#   text = cmd[update:1000] echo "$(~/.config/Scripts/layout-status)‎"
#   color = rgba(242, 243, 244, 0.75)
#   font_size = 15
#   font_family = SF Pro Text
#   position = -150, 512
#   halign = right
#   valign = center

# ------------------ OLD HYPRLOCK --------------------
#source = $HOME/.config/hypr/mocha.conf
#
# $accent = $mauve
# $accentAlpha = $mauveAlpha
# $font = JetBrainsMono Nerd Font
#
# # GENERAL
# general {
#     disable_loading_bar = true
#     hide_cursor = true
#     immediate_render = true
# }
#
# # BACKGROUND
# background {
#     monitor =
#     path = /home/barmanji/Downloads/ColorWall/wallhaven-yq752d.jpg
#     blur_passes = 2
#     color = $base
# }
#
# # TIME
# label {
#     monitor =
#     text = cmd[update:30000] echo "$(date +"%I:%M%p")"
#     color = $text
#     font_size = 60
#     font_family = $font
#     position = -30, 0
#     halign = right
#     valign = top
# }
#
# # DATE
# label {
#     monitor =
#     text = cmd[update:43200000] echo "$(date +"%A, %d %B %Y")"
#     color = $text
#     font_size = 20
#     font_family = $font
#     position = -30, -110
#     halign = right
#     valign = top
# }
#
# # BATTERY
# label {
#     monitor =
#         text = cmd[update:1000] ~/scripts/battery_icon.sh
#         text_align = center
#         color = $accent
#         font_size = 18
#         font_family = $font
#         rotate = 0
#         position = 0, -90
#         halign = center
#         valign = center
# }
#
# # USER AVATAR
#
# # image {
# #     monitor =
# #     path = /home/barmanji/Downloads/ColorWall/wallhaven-vg7lv3.jpg
# #     size = 100
# #     border_color = $accent
# #
# #     position = 0, 75
# #     halign = center
# #     valign = center
# # }
#
# # INPUT FIELD
# input-field {
#     monitor =
#     size = 80, 45
#     outline_thickness = 2
#     dots_size = 0.2
#     dots_spacing = 0.2
#     dots_center = true
#     outer_color = $accent
#     inner_color = $surface0
#     font_color = $text
#     fade_on_empty = false
#     placeholder_text = <span foreground="##$textAlpha"><i>󰌾 Logged in as </i><span foreground="##$accentAlpha">$USER</span></span>
#     hide_input = false
#     check_color = $accent
#     fail_color = $red
#     fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
#     capslock_color = $yellow
#     position = 0, -35
#     halign = center
#     valign = center
# }
# # }
