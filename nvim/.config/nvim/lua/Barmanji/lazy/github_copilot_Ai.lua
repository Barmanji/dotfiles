return {
  -- "github/copilot.vim",
  -- -- Key Fix 1: Only load the plugin when you run the :Copilot command
  -- cmd = "Copilot",
  -- -- Key Fix 2: Set these BEFORE the plugin loads
  -- init = function()
  --   vim.g.copilot_enabled = 0
  -- end
}


-- return {
--   "github/copilot.vim",
--   -- This prevents the plugin from loading (and spawning Node) on startup
--   cmd = "Copilot",
--   -- Optional: Load it only when you enter Insert mode
--   -- event = "InsertEnter",
--   config = function()
--     -- This handles the internal state once you eventually trigger it
--     vim.g.copilot_enabled = false
--   end
--
-- }
--

